apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
    name: portfoliov3-mysqld-exporter
    namespace: monitoring
    labels:
        release: kube-prometheus-stack
spec:
    namespaceSelector:
        matchNames:
            - portfoliov3-api
    selector:
        matchLabels:
            app: mysqld-exporter
    podMetricsEndpoints:
        - path: /metrics
          port: metrics
