apiVersion: v1
kind: Service
metadata:
    name: mysqld-exporter
    namespace: portfoliov3-api
    labels:
        app: mysqld-exporter
spec:
    selector:
        app: mysqld-exporter
    ports:
        - port: 9104
          targetPort: 9104
          name: metrics
